# RAG (Frontend context)

Retrieval-augmented generation (e.g. Qdrant) is a **backend** concern.

The dashboard may later show “sources used” or knowledge-base admin screens via APIs. It must not query the vector DB from the browser.
