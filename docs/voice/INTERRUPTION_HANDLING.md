# Interruption Handling (Frontend context)

Barge-in / interruption handling is owned by the backend voice pipeline.

Future live-call UI may show interruption events; the dashboard must not invent interruption outcomes. Prefer backend event streams or poll APIs.
